export default function DeleteModal({ asset, onConfirm, onClose }) { if (!asset) return null; return <div className="asset-modal-backdrop"><section className="asset-modal"><h2>Delete asset?</h2><p>{asset.name} will be permanently removed from this directory.</p><div><button onClick={onClose}>Cancel</button><button className="danger" onClick={onConfirm}>Delete</button></div></section></div> }
