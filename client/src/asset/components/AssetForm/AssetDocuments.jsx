export default function AssetDocuments({ files = [], onChange }) { return <section className="asset-upload"><div><h3>Documents upload</h3><p>Invoices, warranty records, or service documents.</p></div><label className="asset-file-picker">Choose files<input type="file" multiple hidden onChange={(e) => onChange(Array.from(e.target.files || []).map((file) => file.name))} /></label>{files.length > 0 && <ul className="asset-files">{files.map((file) => <li key={file}>{file}</li>)}</ul>}</section> }
