import { useRef } from 'react'
export default function AssetImageUpload({ value, onChange }) { const input = useRef(null); return <section className="asset-upload"><div><h3>Photo upload</h3><p>Add a clear image for quick identification.</p></div><button type="button" className="asset-image-picker" onClick={() => input.current?.click()}>{value ? <img src={value} alt="Asset preview" /> : <span>Upload photo</span>}</button><input ref={input} type="file" accept="image/*" hidden onChange={(e) => { const file = e.target.files?.[0]; if (file) { const reader = new FileReader(); reader.onload = () => onChange(reader.result); reader.readAsDataURL(file) } }} /></section> }
