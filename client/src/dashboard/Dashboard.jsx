import { useNavigate } from 'react-router-dom'
import { useAuth } from '../auth/hooks/useAuth'
export default function Dashboard() { const { user, logout } = useAuth(); const navigate = useNavigate(); const signOut = async () => { await logout(); navigate('/login') }; return <main className="dashboard"><header><div className="brand"><span className="brand-mark">AF</span><span>AssetFlow<small>Enterprise Asset Management</small></span></div><button className="outline-button" onClick={signOut}>Log out</button></header><section><p className="eyebrow">YOUR WORKSPACE</p><h1>Welcome, {user.fullName}.</h1><p>Your authenticated AssetFlow dashboard is ready.</p><div className="dashboard-grid"><article><span>Access level</span><strong>{user.role}</strong></article><article><span>Account status</span><strong className="active">{user.status}</strong></article><article><span>Security</span><strong>JWT protected</strong></article></div></section></main> }
