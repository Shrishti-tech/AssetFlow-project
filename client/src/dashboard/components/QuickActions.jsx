const actions = [{ title: 'Register Asset', text: 'Add a new item to inventory', icon: '+' }, { title: 'Book Resource', text: 'Reserve a shared resource', icon: '◷' }, { title: 'Raise Maintenance Request', text: 'Report an asset issue', icon: '⌁' }]
export default function QuickActions({ onAction }) { return <section className="dashboard-card quick-actions"><div className="card-heading"><div><h2>Quick actions</h2><p>Common tasks at your fingertips</p></div></div><div>{actions.map((action) => <button key={action.title} onClick={() => onAction(action.title)}><span>{action.icon}</span><div><strong>{action.title}</strong><small>{action.text}</small></div><b>→</b></button>)}</div></section> }
