export default function NotificationPanel({ open, items, onClose, onRead, onReadAll }) { return <section className={`dashboard-card notification-panel ${open ? 'expanded' : ''}`}><div className="card-heading"><div><h2>Notifications</h2><p>Stay on top of your operations</p></div><button className="text-button" onClick={onReadAll}>Mark all read</button>{open && <button className="panel-close" onClick={onClose}>×</button>}</div><div className="notification-list">{items.map((item) => <button className={item.read ? 'read' : ''} key={item.id} onClick={() => onRead(item.id)}><span className={`notification-dot ${item.type}`} /><div><strong>{item.title}</strong><p>{item.text}</p><small>{item.time}</small></div>{!item.read && <i />}</button>)}</div></section> }
