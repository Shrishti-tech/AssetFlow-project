import { useState } from 'react'
const monthly = [42, 59, 47, 70, 62, 82, 76, 93, 86, 105, 96, 118]
const weekly = [55, 76, 48, 92, 70, 86, 62]
export default function DashboardCharts() { const [period, setPeriod] = useState('Month'); const data = period === 'Month' ? monthly : weekly; return <section className="dashboard-card dashboard-charts"><div className="card-heading"><div><h2>Asset utilization</h2><p>Allocated assets compared to available inventory</p></div><div className="segmented-control">{['Week', 'Month'].map((item) => <button onClick={() => setPeriod(item)} className={period === item ? 'selected' : ''} key={item}>{item}</button>)}</div></div><div className="chart-summary"><strong>78.4%</strong><span>+5.2% <small>vs. previous {period.toLowerCase()}</small></span></div><div className="bar-chart">{data.map((height, index) => <div key={index}><i style={{ height: `${height / 1.25}%` }} /><small>{period === 'Month' ? ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'][index] : ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'][index]}</small></div>)}</div></section> }
