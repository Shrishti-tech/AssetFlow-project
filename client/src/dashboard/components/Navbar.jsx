export default function Navbar({ user, search, onSearch, unreadCount, notificationsOpen, onToggleNotifications, onMenu, onLogout }) {
  return <header className="erp-navbar"><button className="menu-button" onClick={onMenu} aria-label="Open menu">☰</button><label className="global-search"><span>⌕</span><input value={search} onChange={(event) => onSearch(event.target.value)} placeholder="Search assets, people, or records..." /></label><div className="nav-actions"><button className={`notification-button ${notificationsOpen ? 'active' : ''}`} onClick={onToggleNotifications} aria-label="Show notifications">♧{unreadCount > 0 && <em>{unreadCount}</em>}</button><div className="user-avatar">{user?.fullName?.slice(0, 1) || 'U'}</div><div className="nav-user"><strong>{user?.fullName || 'Asset manager'}</strong><span>{user?.role || 'Asset manager'}</span></div><button className="logout-link" onClick={onLogout}>Log out</button></div></header>
}
