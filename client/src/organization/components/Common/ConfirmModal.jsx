export default function ConfirmModal({ open, title = 'Remove item?', message, confirmLabel = 'Remove', onConfirm, onClose }) { if (!open) return null; return <div className="org-modal-backdrop" role="presentation"><div className="org-confirm-modal" role="dialog" aria-modal="true"><span className="confirm-icon">!</span><h2>{title}</h2><p>{message}</p><div><button className="org-button secondary" onClick={onClose}>Cancel</button><button className="org-button danger" onClick={onConfirm}>{confirmLabel}</button></div></div></div> }
