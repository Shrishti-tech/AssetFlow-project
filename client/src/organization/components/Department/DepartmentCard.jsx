export default function DepartmentCard({ department, onEdit, onDelete }) { return <article className="department-card"><div className="department-card-icon">{department.code}</div><div><h3>{department.name}</h3><p>{department.location} · {department.members} members</p><span>Led by {department.manager}</span></div><div className="card-actions"><button onClick={() => onEdit(department)}>Edit</button><button className="danger-link" onClick={() => onDelete(department)}>Remove</button></div></article> }
