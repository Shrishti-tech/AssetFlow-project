import { useState } from 'react'
const roles = ['Employee', 'Department Head', 'Asset Manager', 'Admin']
export default function PromoteRoleModal({ employee, onSave, onClose }) { const [role, setRole] = useState(employee.role); if (!employee) return null; return <div className="org-modal-backdrop"><form className="org-confirm-modal promote-modal" onSubmit={(e) => { e.preventDefault(); onSave(role) }}><span className="confirm-icon">↑</span><h2>Change employee role</h2><p>Select a new role for <b>{employee.name}</b>. This is local dummy data only.</p><label>New role<select value={role} onChange={(e) => setRole(e.target.value)}>{roles.map((item) => <option key={item}>{item}</option>)}</select></label><div><button type="button" className="org-button secondary" onClick={onClose}>Cancel</button><button className="org-button">Update role</button></div></form></div> }
