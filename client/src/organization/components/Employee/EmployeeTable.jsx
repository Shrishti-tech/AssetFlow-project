export default function EmployeeTable({ items, onEdit, onPromote, onDelete }) { return <div className="org-table-wrap"><table className="org-table employee-table"><thead><tr><th>Employee</th><th>Department</th><th>Role</th><th>Status</th><th /></tr></thead><tbody>{items.map((item) => <tr key={item.id}><td><div className="table-person"><span className="employee-avatar">{item.initials}</span><div><b>{item.name}</b><small>{item.email}</small></div></div></td><td>{item.department}</td><td><span className={`role-pill ${item.role.toLowerCase().replaceAll(' ', '-')}`}>{item.role}</span></td><td><span className={`org-status ${item.status.toLowerCase()}`}>{item.status}</span></td><td className="table-actions"><button onClick={() => onEdit(item)}>Edit</button><button onClick={() => onPromote(item)}>Change role</button><button onClick={() => onDelete(item)}>Remove</button></td></tr>)}</tbody></table></div> }
