export default function EmployeeCard({ employee, onEdit, onPromote, onDelete }) { return <article className="employee-card"><span className="employee-avatar">{employee.initials}</span><div><h3>{employee.name}</h3><p>{employee.email}</p><span>{employee.department}</span></div><span className={`role-pill ${employee.role.toLowerCase().replaceAll(' ', '-')}`}>{employee.role}</span><div className="card-actions"><button onClick={() => onEdit(employee)}>Edit</button><button onClick={() => onPromote(employee)}>Role</button><button className="danger-link" onClick={() => onDelete(employee)}>Remove</button></div></article> }
