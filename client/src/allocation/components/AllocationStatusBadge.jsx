const styles = { active: ['Allocated', 'blue'], returned: ['Available', 'green'], requested: ['Requested', 'yellow'], pending: ['Pending approval', 'orange'], overdue: ['Overdue', 'red'], completed: ['Completed', 'green'], rejected: ['Rejected', 'red'] }
export default function AllocationStatusBadge({ status }) { const [label, tone] = styles[status] || [status || 'Unknown', 'gray']; return <span className={`allocation-badge ${tone}`}>{label}</span> }
